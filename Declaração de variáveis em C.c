#include <stdio.h>
int main (){
  
  /*
			VARIÁVEIS.
	1) Toda variável possui um nome
	2) Toda variável possui um tipo
  	3) Toda variável possui um tamanho
  	4) Toda variável possui um valor 
  	
  	        sobre o valor das variáveis.
  				a) É temporário.
				b) Quando o pc é desligado ou o programa é fechado, tudo o que estiver na memória não será salvo.      
				c) Valores armazenados na memória são voláteis.
				d) O valor antigo da variável será sobreposto.
  */
  
  //exemplo de declaração de variável.
  // 		tipo NomedaVariavel = valor padrão  ;
		int num, num2 = 0;
		double numeroVerificador;
		//não pode começar com numeros.
		//int 2mumeros
		int _2numeros
		// nenhumm caractere especial a não ser o _ 
		//não pode conter espaço no nome das variáveis,, substituir por _.
		
					/*			TIPOS DE VARIÁVEIS.
					
					1) char <- caracteres {a,b,c,d,e...} 1byte ou seja 8 Bits
					2) int <- numeros positivos e negativos  2 bytes - -32786 +32786
					3) float <- numeros com casa decimais {10,3 . 5,5 . 2,1....} 4bytes 
					4) Void <- vazio
					5) Double <- permite até 15 casas decimais.
					
					
					*/
		
		
		



}